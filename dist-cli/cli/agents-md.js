"use strict";
// Generated content for ~/.flow/AGENTS.md — the contract a cold agent reads to
// orient itself. Written to disk by `flow init`.
Object.defineProperty(exports, "__esModule", { value: true });
exports.AGENTS_MD = void 0;
exports.AGENTS_MD = "# Flow \u2014 agent contract\n\nYou are working in `~/.flow/`. This is a single person's task list. It exists because\nevery other tool they tried required a decision at the moment of capture, and that is\nthe thing that broke. Your job is to carry the context they don't want to carry, so\nthat capturing a thought stays free.\n\nRead this whole file before writing anything.\n\n## The one rule\n\n**Never add a decision at capture time.** Everything you do happens at review time or in\nthe background. If a change of yours would make the owner stop and choose something\nbefore they can write a thought down, it does not belong here.\n\n## How to write\n\nUse the `flow` CLI. Never edit `items.json` by hand \u2014 the CLI writes atomically, journals\nevery change, and keeps the git history honest. A hand-edit skips all three.\n\nIdentify yourself on every call:\n\n```bash\nexport FLOW_ACTOR=agent:my-name     # or pass --actor agent:my-name\n```\n\n## You do not set priority\n\nThis is the part most likely to surprise you. **Do not call `flow set <id> priority`.**\n\nPriority is computed, not decided. You report what you observed; a scorer in\n`weights.json` turns observations into a ranking. The split exists so the owner can read\nwhy something ranked where it did, retune it by editing one file, and get the same answer\ntwice. A number you invented has none of those properties.\n\nSo: **emit signals.**\n\n```bash\nflow signal meeting 'Trading fee automation sync, Thu 2pm' \\\n  --project tradingfees --at 2026-09-17T14:00:00 --source calendar\n\nflow signal unblocked 'code review approved in #eng' \\\n  --tags acceptance --source slack:#eng\n\nflow signal blocking 'three other items are waiting on this' --items <id>\nflow signal deadline 'contract renewal is the 30th' --project billing --at 2026-09-30\nflow signal mentioned 'came up twice in standup' --match 'deep link'\n```\n\n### Targeting\n\nA signal applies to every item it matches. Use the narrowest thing that is true:\n\n| flag | matches |\n|---|---|\n| `--items id1,id2` | exactly those items |\n| `--project p` | items in that project |\n| `--cluster c` | items you grouped under that cluster |\n| `--tags a,b` | items carrying any of those tags |\n| `--match 'text'` | items whose title or description contains it |\n\n### Timing\n\n- `--at <when>` for something that happens at a time. Urgency **rises** as it approaches,\n  stays hot for a day afterwards for the follow-ups, then fades. Use it for meetings and\n  deadlines.\n- No `--at` means \"I observed this now\". It decays from today. Use it for reviews landing,\n  mentions, anything that is news rather than an appointment.\n- `--expires <when>` if you know it stops mattering at a hard boundary.\n\n### The note is the product\n\nThe `note` is shown to the owner directly underneath the item as the reason it ranked\nwhere it did. Write it for them:\n\n- One short sentence, no preamble, no \"I noticed that\".\n- Say the *fact*, not your inference. \"Code review approved in #eng\" is a fact they can\n  check. \"This is now high priority\" is you doing the scoring's job.\n- If you can't state a fact plainly, you don't have a signal. Don't emit one.\n\n### Kinds and weights\n\nThe default weights are in `~/.flow/weights.json` and the owner tunes them:\n\n| kind | default |\n|---|---|\n| `deadline` | 0.9 |\n| `meeting` | 0.8 |\n| `blocking` | 0.7 |\n| `unblocked` | 0.6 |\n| `mentioned` | 0.3 |\n| anything else | 0.4 |\n\nInvent a new kind if none fits \u2014 it scores at the default until the owner gives it a\nweight. Several signals on one item accumulate without ever exceeding 1.\n\nCheck your work with `flow score --explain`, which prints every item with the signals\nthat moved it and by how much.\n\n## What you do set\n\n```bash\nflow set <id> cluster android-attribution\n```\n\n`cluster` groups captures that are really the same thought written three different ways\nacross three weeks. The owner cannot see that; you can.\n\nDo not merge them. Do not delete any of them. Clustering is a label, not a decision \u2014 the\nowner decides what to do about it, and can only do that if all three are still there.\n\n## Capturing\n\nOne line, no dependencies, from anywhere:\n\n```bash\necho 'follow up on android attribution #referral @bug' >> ~/.flow/inbox.jsonl\n```\n\nFirst `#word` is the project, any `@word` or later `#word` is a tag, the rest is the\ntitle. Drained next time the owner opens the list, or on `flow list`.\n\nUse `flow add` when you want the id back or need `--source`.\n\n## Reading\n\n```bash\nflow list --json                 # open items, in the owner's sort order\nflow list --json --status all\nflow list --json --stale         # todo and older than 14 days\nflow signals --json              # what's currently in play\nflow score --explain             # the ranking, with its reasoning\n```\n\n## Cleaning up\n\nYou may **propose** cleanup. You may not perform it.\n\n```bash\nflow tidy                        # what looks finished\nflow tidy --propose              # turn those into asks the owner can accept\n```\n\n`flow tidy` finds two things: projects where the work is done and nothing new has arrived\nin weeks, and done items old enough that nobody is coming back to them. `--propose` files\nthem as asks carrying an archive action, so the owner accepts with one keystroke.\n\nArchived items are not deleted \u2014 they go to `archive.jsonl` in full, and a git commit is\ntaken immediately before. Both are recoverable.\n\n## Asking\n\nWhen you genuinely cannot tell, ask instead of guessing:\n\n```bash\nflow ask 'Is the Android attribution work still live?' --item <id>\nflow suggest 'These three look like one thing' --items id1,id2,id3\n```\n\nEach ask is a decision handed back \u2014 the thing this tool exists to avoid. The bar is\nhigh: ask only when the answer changes what you do, and never queue more than three or\nfour at once.\n\n## Before every run, read the feedback\n\n```bash\nflow feedback --json --since <ts of your last run>\n```\n\nThis is the owner pushing back. Four kinds:\n\n| kind | what it means |\n|---|---|\n| `undo` | You made this change and they reversed it. **Do not make it again.** |\n| `answer` | They answered an ask. Use it \u2014 it's the best signal you get. |\n| `pin` | They pinned an item. Never touch its priority or ordering. |\n| `dismiss` | They dismissed an ask. Don't re-ask it. |\n\nIf you believe an undone change was right, raise it with `flow ask` rather than redoing\nit. Silently reapplying something the owner reversed is the fastest way to get this whole\nsystem turned off.\n\n## Undo and history\n\nEvery change is journaled to `journal.jsonl` with before and after, and `~/.flow` is a git\nrepo. `flow undo [n]` reverses recent changes; `flow snapshot -m '...'` commits. This\nsafety net is what makes your write access acceptable \u2014 do not work around it.\n\n## The files\n\n| file | who writes | shape |\n|---|---|---|\n| `items.json` | CLI only | `{version, items[]}` \u2014 canonical |\n| `inbox.jsonl` | anyone, append only | one raw capture line per row |\n| `signals.jsonl` | you, append only | observations that drive the ranking |\n| `weights.json` | the owner | how signals turn into priority |\n| `journal.jsonl` | CLI only, append only | every mutation, with before/after |\n| `feedback.jsonl` | the owner, append only | your learning signal \u2014 read it |\n| `asks.jsonl` | you | questions and proposals awaiting an answer |\n| `archive.jsonl` | CLI only, append only | items removed from the list, in full |\n| `digest.md` | you | optional morning brief, plain markdown |\n\n## Item shape\n\n```ts\n{\n  id, title, description, project?, tags[], status: \"todo\"|\"done\", createdAt, doneAt?,\n\n  priority?,        // 0..1          computed by the scorer, NOT by you\n  why?,             // one line      assembled from the signals that fired\n  cluster?,         // slug          you\n  source?,          // \"linear:ENG-41\", \"slack:C123/p456\", \"calendar:...\"\n  agentTouchedAt?,  //               set for you automatically\n\n  pinned?           // owner's veto \u2014 leave the item entirely alone\n}\n```\n\n## Things not to do\n\n- Don't set `priority` or write `why` directly. Emit signals and let the scorer do it.\n- Don't create items from a feed without being asked. Slack and email are high volume and\n  low signal; if everything becomes an item, nothing is.\n- Don't notify. Your output is pull, not push \u2014 it improves the list, and opening the list\n  is the trigger. Anything that interrupts sixteen times a day gets muted within a week.\n- Don't mark anything done. Finishing is the owner's to declare.\n- Don't delete anything. Propose an archive instead.\n- Don't touch a pinned item.\n";
