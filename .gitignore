node_modules/

# Build output. dist-cli/flow.cjs bakes a build timestamp, so it differs on every
# build even when nothing changed — tracking it would add a spurious diff to every
# commit. Rebuild with `npm run install:cli`.
dist/
dist-cli/

# Raycast
.raycast/
